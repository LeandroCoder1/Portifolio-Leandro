<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Portfólio — Leandro Sousa</title>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>

<body style="background-color: #000000 !important; color: #FFFFFF;">
    <main class="container my-5" id="mainContent" style="">
        @RenderBody()
    </main>

    <div class="box">

        <h3 class="name">
            Leandro Sousa
        </h3>
        <div style="color: gray; font-size: 12px">
            <footer id="pageFooter" class="bg-dark text-white py-4"
                    style="position: fixed; left: 10px; bottom: 8px; width: 100%; z-index: 9999; font-family: cursive;
">
                <div class="container d-flex justify-content-between">
                    <div>© @DateTime.Now.Year — Leandro Sousa</div>
                </div>
            </footer>
        </div>
    </div>

    <div class="nav-btns">
        <a class="btn" href="#home">Ínicio</a>
        <a class="btn" href="#about">Sobre mim</a>
        <a class="btn" href="#projects">Projetos</a>
        <a class="btn" href="#contact">Contatos</a>
    </div>


    <main>
        <section id="home" class="tab-section">
            <p>Conteúdo inicial...</p>
        </section>

        <section id="about" class="hero">
            <h1>Sobre mim</h1>
            <p>Informações sobre mim...</p>
        </section>

        <section id="projects" class="tab-section">
            <h1>Projetos</h1>
            <p>Lista de projetos...</p>
        </section>

        <section id="contact" class="tab-section">
            <h1>Contato</h1>
            <p>Formulário de contato...</p>
        </section>
    </main>




    <script src="~/js/site.js"></script>

    <!-- Script mínimo que evita que o footer fixo sobreponha conteúdo (ajusta padding-bottom dinâmico) -->
@*     <script>
        (function () {
          const footer = document.getElementById('pageFooter');
          const main = document.getElementById('mainContent');

          function adjustPadding() {
            if (!footer) return;
            const footHeight = footer.offsetHeight + 16; // pequeno buffer
            // aplica padding-bottom no body (ou no main) para garantir que nada fique coberto
            document.body.style.paddingBottom = footHeight + 'px';
          }

          // executa ao carregar e ao redimensionar
          window.addEventListener('load', adjustPadding);
          window.addEventListener('resize', adjustPadding);
          // também observa mudanças no layout do footer (caso o conteúdo do footer mude)
          if (window.ResizeObserver) {
            new ResizeObserver(adjustPadding).observe(footer);
          }
        })();
    </script> *@

</body>
</html>
